# Pinned to the versions verified against this codebase — bump deliberately,
# not via rebuild roulette. chromadb in particular: major bumps can invalidate
# the on-disk embedding store in data/chroma.
py-cord==2.8.0
openai==2.15.0
aiohttp==3.13.5
chromadb==1.5.9
pillow==12.1.0
pypdf==6.12.2
python-dotenv==1.2.2
psutil==7.2.1
google-api-python-client==2.187.0
